CREATE TABLE [dbo].[rezervDate] (
    [nume]      VARCHAR (50) NOT NULL,
    [prenume]   VARCHAR (50) NULL,
    [telefon]   INT          NULL,
    [tipcamera] INT          NULL,
    [durata] INT NULL, 
    PRIMARY KEY CLUSTERED ([nume] ASC)
);

